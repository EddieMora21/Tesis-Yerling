@model SistemaNomina.Incapacidades

@{
    ViewBag.Title = "Detalles de Incapacidad";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid">
    <div class="row">
        <div class="col-md-12">
            <!-- Header -->
            <div class="page-header mb-4">
                <div class="d-flex justify-content-between align-items-center">
                    <div>
                        <h2 class="page-title">
                            <i class="fas fa-eye text-info"></i>
                            Detalles de Incapacidad
                        </h2>
                        <p class="text-muted">Información completa del registro de incapacidad</p>
                    </div>
                    <div class="page-actions no-print">
                        @Html.ActionLink("Volver a Lista", "Index", null, new { @class = "btn btn-secondary btn-lg" })
                        @Html.ActionLink("Editar", "Edit", new { id = Model.id_incapacidad }, new { @class = "btn btn-warning btn-lg text-dark" })
                        @Html.ActionLink("Eliminar", "Delete", new { id = Model.id_incapacidad }, new { @class = "btn btn-danger btn-lg" })
                    </div>
                </div>
            </div>

            <!-- Información principal -->
            <div class="row">
                <div class="col-md-8">
                    <!-- Tarjeta principal de información -->
                    <div class="card mb-4">
                        <div class="card-header bg-info text-white">
                            <h5 class="card-title mb-0">
                                <i class="fas fa-user-injured"></i>
                                Información de la Incapacidad
                                <span class="badge bg-light text-dark ms-2">ID: @Model.id_incapacidad</span>
                            </h5>
                        </div>
                        <div class="card-body">
                            <div class="row">
                                <!-- Datos del empleado -->
                                <div class="col-md-6">
                                    <div class="detail-section">
                                        <h6 class="section-title">
                                            <i class="fas fa-user text-primary"></i> Empleado
                                        </h6>
                                        <div class="detail-item">
                                            <div class="d-flex align-items-center mb-3">
                                                <div class="avatar-lg bg-primary text-white rounded-circle d-flex align-items-center justify-content-center me-3">
                                                    @if (Model.Empleados != null && !string.IsNullOrEmpty(Model.Empleados.nombre1) && !string.IsNullOrEmpty(Model.Empleados.apellido1))
                                                    {
                                                        @Model.Empleados.nombre1.Substring(0, 1)@Model.Empleados.apellido1.Substring(0, 1)
                                                    }
                                                    else
                                                    {
                                                        <text>??</text>
                                                    }
                                                </div>
                                                <div>
                                                    <h5 class="mb-1">
                                                        @if (Model.Empleados != null)
                                                        {
                                                            @(Model.Empleados.nombre1 ?? "Sin nombre") @(Model.Empleados.apellido1 ?? "Sin apellido")
                                                        }
                                                        else
                                                        {
                                                            <span class="text-muted">Sin empleado asignado</span>
                                                        }
                                                    </h5>
                                                    <p class="text-muted mb-0">
                                                        <i class="fas fa-id-card me-1"></i>
                                                        Cédula: @(Model.Empleados?.cedula ?? "No disponible")
                                                    </p>
                                                    @if (Model.Empleados?.Puestos != null && !string.IsNullOrEmpty(Model.Empleados.Puestos.nombre_puesto))
                                                    {
                                                        <p class="text-muted mb-0">
                                                            <i class="fas fa-briefcase me-1"></i>
                                                            @Model.Empleados.Puestos.nombre_puesto
                                                        </p>
                                                    }
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>

                                <!-- Tipo y estado -->
                                <div class="col-md-6">
                                    <div class="detail-section">
                                        <h6 class="section-title">
                                            <i class="fas fa-clipboard-list text-success"></i> Clasificación
                                        </h6>
                                        <div class="detail-item">
                                            <label class="detail-label">Tipo de Incapacidad:</label>
                                            <div class="detail-value">
                                                @if (Model.TipoIncapacidades != null && !string.IsNullOrEmpty(Model.TipoIncapacidades.nombre))
                                                {
                                                    <span class="badge bg-info text-white">@Model.TipoIncapacidades.nombre</span>
                                                }
                                                else
                                                {
                                                    <span class="text-muted">No especificado</span>
                                                }
                                            </div>
                                        </div>
                                        <div class="detail-item">
                                            <label class="detail-label">Estado Actual:</label>
                                            <div class="detail-value">
                                                @{
                                                    string badgeClass = "bg-secondary";
                                                    string iconClass = "fas fa-question";
                                                    string estadoNombre = "Sin estado";

                                                    if (Model.Estados != null && !string.IsNullOrEmpty(Model.Estados.nombre))
                                                    {
                                                        estadoNombre = Model.Estados.nombre;
                                                        switch (Model.Estados.nombre.ToLower())
                                                        {
                                                            case "pendiente":
                                                                badgeClass = "bg-warning text-dark";
                                                                iconClass = "fas fa-clock";
                                                                break;
                                                            case "aprobado":
                                                                badgeClass = "bg-success";
                                                                iconClass = "fas fa-check";
                                                                break;
                                                            case "rechazado":
                                                                badgeClass = "bg-danger";
                                                                iconClass = "fas fa-times";
                                                                break;
                                                        }
                                                    }
                                                }
                                                <span class="badge @badgeClass">
                                                    <i class="@iconClass me-1"></i>
                                                    @estadoNombre
                                                </span>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <hr class="my-4">

                            <!-- Fechas y duración -->
                            <div class="row">
                                <div class="col-md-12">
                                    <div class="detail-section">
                                        <h6 class="section-title">
                                            <i class="fas fa-calendar-alt text-warning"></i> Período de Incapacidad
                                        </h6>
                                        <div class="row">
                                            <div class="col-md-3">
                                                <div class="detail-item">
                                                    <label class="detail-label">Fecha Inicio:</label>
                                                    <div class="detail-value">
                                                        <i class="fas fa-calendar-plus text-success me-1"></i>
                                                        <strong>@Model.fecha_inicio.ToString("dd/MM/yyyy")</strong>
                                                    </div>
                                                </div>
                                            </div>
                                            <div class="col-md-3">
                                                <div class="detail-item">
                                                    <label class="detail-label">Fecha Fin:</label>
                                                    <div class="detail-value">
                                                        <i class="fas fa-calendar-minus text-danger me-1"></i>
                                                        <strong>@Model.fecha_fin.ToString("dd/MM/yyyy")</strong>
                                                    </div>
                                                </div>
                                            </div>
                                            <div class="col-md-3">
                                                <div class="detail-item">
                                                    <label class="detail-label">Duración:</label>
                                                    <div class="detail-value">
                                                        <span class="badge bg-primary fs-6">
                                                            <i class="fas fa-hourglass-half me-1"></i>
                                                            @(Model.dias_incapacidad ?? 0) días
                                                        </span>
                                                    </div>
                                                </div>
                                            </div>
                                            <div class="col-md-3">
                                                <div class="detail-item">
                                                    <label class="detail-label">Días transcurridos:</label>
                                                    <div class="detail-value">
                                                        @{
                                                            var hoy = DateTime.Now.Date;
                                                            var fechaInicio = Model.fecha_inicio.Date;
                                                            var fechaFin = Model.fecha_fin.Date;

                                                            var diasTranscurridos = (hoy - fechaInicio).Days;
                                                            if (diasTranscurridos < 0) { diasTranscurridos = 0; }

                                                            var diasRestantes = (fechaFin - hoy).Days;
                                                            if (diasRestantes < 0) { diasRestantes = 0; }
                                                        }
                                                        <span class="text-info">
                                                            <i class="fas fa-clock me-1"></i>
                                                            @diasTranscurridos días
                                                        </span>
                                                        @if (diasRestantes > 0)
                                                        {
                                                            <small class="text-muted d-block">@diasRestantes días restantes</small>
                                                        }
                                                        else if (hoy > fechaFin)
                                                        {
                                                            <small class="text-success d-block">Incapacidad finalizada</small>
                                                        }
                                                        else if (hoy >= fechaInicio && hoy <= fechaFin)
                                                        {
                                                            <small class="text-warning d-block">En curso</small>
                                                        }
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            @if (!string.IsNullOrEmpty(Model.descripcion))
                            {
                                <hr class="my-4">
                                <!-- Descripción -->
                                <div class="detail-section">
                                    <h6 class="section-title">
                                        <i class="fas fa-comment-medical text-info"></i> Descripción
                                    </h6>
                                    <div class="description-box">
                                        @Model.descripcion
                                    </div>
                                </div>
                            }
                        </div>
                    </div>
                </div>

                <!-- Información adicional -->
                <div class="col-md-4">
                    <!-- Datos administrativos -->
                    <div class="card mb-4">
                        <div class="card-header bg-secondary text-white">
                            <h6 class="card-title mb-0">
                                <i class="fas fa-cog"></i> Información Administrativa
                            </h6>
                        </div>
                        <div class="card-body">
                            @if (!string.IsNullOrEmpty(Model.numero_boleta))
                            {
                                <div class="detail-item mb-3">
                                    <label class="detail-label">Número de Boleta:</label>
                                    <div class="detail-value">
                                        <span class="font-monospace fw-bold text-primary">@Model.numero_boleta</span>
                                    </div>
                                </div>
                            }

                            <div class="detail-item mb-3">
                                <label class="detail-label">Fecha de Registro:</label>
                                <div class="detail-value">
                                    <i class="fas fa-calendar-plus me-1"></i>
                                    @if (Model.fecha_registro!= null)
                                    {
                                        @Model.fecha_registro.Value.ToString("dd/MM/yyyy HH:mm")
                                    }
                                    else
                                    {
                                        <span class="text-muted">No disponible</span>
                                    }
                                </div>
                            </div>

                            <div class="detail-item">
                                <label class="detail-label">ID del Registro:</label>
                                <div class="detail-value">
                                    <span class="font-monospace">#@Model.id_incapacidad.ToString("D6")</span>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Acciones rápidas -->
                    <div class="card no-print">
                        <div class="card-header bg-dark text-white">
                            <h6 class="card-title mb-0">
                                <i class="fas fa-tools"></i> Acciones Rápidas
                            </h6>
                        </div>
                        <div class="card-body">
                            <div class="d-grid gap-2">
                                @Html.ActionLink("✏️ Editar Incapacidad", "Edit", new { id = Model.id_incapacidad }, new { @class = "btn btn-warning" })
                                <button type="button" class="btn btn-info" onclick="imprimirReporte()">
                                    📋 Imprimir Reporte
                                </button>
                                <hr>
                                @Html.ActionLink("🗑️ Eliminar Registro", "Delete", new { id = Model.id_incapacidad }, new { @class = "btn btn-outline-danger", @onclick = "return confirm('¿Está seguro de eliminar esta incapacidad?')" })
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Información solo visible en impresión -->
            <div class="d-none print-only">
                <hr style="margin-top: 2rem;">
                <div class="row">
                    <div class="col-12 text-center">
                        <p class="mb-1"><strong>Sistema de Gestión de Recursos Humanos</strong></p>
                        <p class="mb-1">Smart Building Solutions - Santa Ana, Costa Rica</p>
                        <p class="mb-0 text-muted">Reporte de Incapacidad - Generado automáticamente</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <style>
        /* Estilos para Details */
        .page-header {
            background: linear-gradient(135deg, #17a2b8 0%, #138496 100%);
            color: white;
            padding: 2rem;
            border-radius: 0.5rem;
            margin-bottom: 2rem;
        }

        .page-title {
            font-size: 2rem;
            font-weight: 600;
            margin-bottom: 0.5rem;
        }

        .card {
            border: none;
            box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075);
            border-radius: 0.5rem;
        }

        .detail-section {
            margin-bottom: 1.5rem;
        }

        .section-title {
            color: #495057;
            font-weight: 600;
            margin-bottom: 1rem;
            padding-bottom: 0.5rem;
            border-bottom: 2px solid #dee2e6;
        }

        .detail-item {
            margin-bottom: 1rem;
        }

        .detail-label {
            font-weight: 600;
            color: #6c757d;
            font-size: 0.875rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: 0.25rem;
            display: block;
        }

        .detail-value {
            font-size: 1rem;
            color: #212529;
            font-weight: 500;
        }

        .avatar-lg {
            width: 60px;
            height: 60px;
            font-size: 1.5rem;
            font-weight: 600;
        }

        .description-box {
            background-color: #f8f9fa;
            padding: 1rem;
            border-radius: 0.5rem;
            border-left: 4px solid #17a2b8;
            font-style: italic;
            color: #495057;
        }

        .badge {
            font-size: 0.875rem;
            padding: 0.5rem 0.75rem;
        }

        .btn-lg {
            padding: 0.75rem 1.5rem;
            font-size: 1.1rem;
            font-weight: 600;
            border-radius: 0.5rem;
        }

        .font-monospace {
            font-family: 'Courier New', monospace;
        }

        .d-grid .btn {
            margin-bottom: 0.5rem;
        }

        .print-only {
            display: none;
        }

        /* Estilos específicos para impresión */
        @@media print {
            /* Ocultar elementos no necesarios */
            .no-print,
            .page-actions,
            .btn,
            nav,
            .navbar,
            .sidebar,
            .footer {
                display: none !important;
            }

            /* Mostrar elementos solo de impresión */
            .print-only {
                display: block !important;
                font-size: 10pt;
                margin-top: 1rem;
                padding-top: 1rem;
                border-top: 1px solid #ddd;
            }

            /* Ajustar página */
            @@page {
                margin: 1.5cm;
                size: A4;
            }

            body {
                font-size: 12pt;
                line-height: 1.4;
                color: #000 !important;
                background: white !important;
            }

            /* Header para impresión */
            .page-header {
                background: none !important;
                color: #000 !important;
                border-bottom: 2px solid #000;
                padding: 1rem 0;
                margin-bottom: 1.5rem;
            }

            .page-title {
                font-size: 18pt;
                color: #000 !important;
            }

            /* Cards */
            .card {
                border: 1px solid #ddd !important;
                box-shadow: none !important;
                margin-bottom: 1rem;
                page-break-inside: avoid;
            }

            .card-header {
                background: #f8f9fa !important;
                color: #000 !important;
                border-bottom: 1px solid #ddd;
                padding: 0.75rem;
            }

            .card-body {
                padding: 1rem;
            }

            /* Secciones */
            .section-title {
                color: #000 !important;
                border-bottom: 1px solid #000;
                font-size: 14pt;
                margin-bottom: 0.75rem;
            }

            .detail-label {
                font-weight: bold;
                color: #000 !important;
                font-size: 10pt;
            }

            .detail-value {
                color: #000 !important;
                font-size: 11pt;
                margin-bottom: 0.5rem;
            }

            /* Badges */
            .badge {
                border: 1px solid #000 !important;
                background: white !important;
                color: #000 !important;
                padding: 0.25rem 0.5rem;
            }

            /* Avatar */
            .avatar-lg {
                border: 2px solid #000;
                background: white !important;
                color: #000 !important;
            }

            /* Description box */
            .description-box {
                background: #f8f9fa !important;
                border: 1px solid #ddd;
                border-left: 4px solid #000;
            }

            /* Ocultar iconos de FontAwesome en impresión */
            .fas, .far, .fab {
                display: none;
            }

            /* Mejorar espaciado */
            .row {
                margin: 0;
            }

            .col-md-6, .col-md-3, .col-md-4, .col-md-8, .col-md-12 {
                padding: 0.25rem;
            }

            /* Información de pie de página */
            .print-footer {
                position: fixed;
                bottom: 1cm;
                left: 0;
                right: 0;
                text-align: center;
                font-size: 10pt;
                border-top: 1px solid #ddd;
                padding-top: 0.5rem;
            }
        }

        /* Responsivo */
        @@media (max-width: 768px) {
            .page-header {
                padding: 1.5rem;
            }

            .page-title {
                font-size: 1.5rem;
            }

            .page-actions {
                margin-top: 1rem;
            }

            .page-actions .btn {
                display: block;
                width: 100%;
                margin: 0.25rem 0;
            }

            .avatar-lg {
                width: 50px;
                height: 50px;
                font-size: 1.25rem;
            }

            .detail-value {
                font-size: 0.9rem;
            }
        }
    </style>

    <script>
        $(document).ready(function () {
            // Mejorar tooltips
            $('[title]').tooltip();

            // Animaciones suaves
            $('.card').hide().fadeIn(500);
        });

        // Función mejorada para imprimir
        function imprimirReporte() {
            // Añadir información adicional para la impresión
            var fechaImpresion = new Date().toLocaleDateString('es-CR');
            var horaImpresion = new Date().toLocaleTimeString('es-CR');

            // Crear elemento temporal para información de impresión
            var printInfo = $('<div class="print-footer">').html(
                'Impreso el ' + fechaImpresion + ' a las ' + horaImpresion +
                ' | Sistema de Gestión de RRHH - Smart Building Solutions'
            );

            // Añadir al body temporalmente
            $('body').append(printInfo);

            // Imprimir
            window.print();

            // Remover el elemento temporal después de imprimir
            setTimeout(function () {
                printInfo.remove();
            }, 1000);
        }
    </script>
}